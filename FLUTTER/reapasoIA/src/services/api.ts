import { User, Book, Loan, LoginCredentials, BookFormData, LoanFormData } from '../types';

// Importar la API simplificada
import { api as simpleApi } from '../config/api';

// Re-exportar la API simplificada
export const api = simpleApi;
